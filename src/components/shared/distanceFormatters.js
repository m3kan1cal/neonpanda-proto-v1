/**
 * Distance unit formatting utilities
 *
 * Shared formatters for distance units used across discipline section components.
 */

export const formatDistanceUnit = (unit) => (unit === "km" ? "km" : "mi");
